/**
 * The panel contract fixture set (AC-P2.1). Exported so P3's scenarios and the
 * adapter-contract suite can reuse the canonical shapes rather than inventing
 * parallel ones.
 */
export * from "./valid";
export * from "./invalid";
