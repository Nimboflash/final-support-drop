/**
 * @drop/contracts — Zod schemas and generated JSON Schema.
 * Safe placeholder only (ticket 0.1 AC-5): a typed export proving the package
 * boundary exists. Real content arrives with this package's owning ticket.
 */
export interface ContractsPackageInfo {
  readonly name: "@drop/contracts";
  readonly placeholder: true;
}

export const packageInfo: ContractsPackageInfo = {
  name: "@drop/contracts",
  placeholder: true,
};
