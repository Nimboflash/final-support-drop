/**
 * @drop/ai-gateway — provider-neutral model profiles and adapters.
 * Safe placeholder only (ticket 0.1 AC-5): a typed export proving the package
 * boundary exists. Real content arrives with this package's owning ticket.
 */
export interface AiGatewayPackageInfo {
  readonly name: "@drop/ai-gateway";
  readonly placeholder: true;
}

export const packageInfo: AiGatewayPackageInfo = {
  name: "@drop/ai-gateway",
  placeholder: true,
};
