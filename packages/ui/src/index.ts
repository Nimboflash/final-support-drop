/**
 * @drop/ui — shadcn components, DROP tokens and FA/RTL primitives.
 * Safe placeholder only (ticket 0.1 AC-5): a typed export proving the package
 * boundary exists. Real content arrives with this package's owning ticket.
 */
export interface UiPackageInfo {
  readonly name: "@drop/ui";
  readonly placeholder: true;
}

export const packageInfo: UiPackageInfo = {
  name: "@drop/ui",
  placeholder: true,
};
