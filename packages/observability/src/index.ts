/**
 * @drop/observability — structured logs, metrics and tracing IDs.
 * Safe placeholder only (ticket 0.1 AC-5): a typed export proving the package
 * boundary exists. Real content arrives with this package's owning ticket.
 */
export interface ObservabilityPackageInfo {
  readonly name: "@drop/observability";
  readonly placeholder: true;
}

export const packageInfo: ObservabilityPackageInfo = {
  name: "@drop/observability",
  placeholder: true,
};
